# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# dependencies & build output
node_modules/
dist/

# local env / scratch
*.local
.physics-probe/

# map directory storage (the shared-maps JSON the server writes at runtime)
server/data/
